package net.ictcampus.piedometre.util.runnable;

import android.os.SystemClock;

public class timeRunnable implements Runnable {
    @Override
    public void run() {
        /*
        milliSecondTime = SystemClock.uptimeMillis() - startTime;
        updateTime = timeBuffer + milliSecondTime;
        seconds = (int) (updateTime / 1000);
        minutes = seconds / 60;
        seconds = seconds % 60;
        milliseconds = (int) (updateTime % 1000);

        timerTextView.setText(String.format("%d:%02d:%02d", minutes, seconds, milliseconds));
        // String.format(R.string.start_time_format, minutes, seconds, milliseconds));
        handler.postDelayed(this, 0);

         */
    }
}
